services:
  - type: web
    name: puppeteer-server
    env: node
    plan: starter
    autoDeploy: true
    healthCheckPath: /

    buildCommand: |
      echo "📦 Installing dependencies only..."
      npm install

    startCommand: |
      echo "🌐 Installing Chrome at runtime..."
      npx puppeteer browsers install chrome

      echo "📁 Chrome installed — Listing directory:"
      ls -R /opt/render/.cache/puppeteer || true

      echo "🚀 Starting Puppeteer server..."
      node puppeteer-server.js
